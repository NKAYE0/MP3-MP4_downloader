namespace YtTikDownloader.App.ViewModels;

/// <summary>Simple value+label pair for ComboBoxes where the stored value and the displayed text differ.</summary>
public sealed record ComboOption(string Value, string Label);
