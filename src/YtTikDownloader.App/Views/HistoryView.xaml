<UserControl x:Class="YtTikDownloader.App.Views.HistoryView"
             x:Name="RootControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:YtTikDownloader.App.Converters">

    <UserControl.Resources>
        <conv:FileSizeConverter x:Key="FileSize"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Search by title or URL"/>
            <Button Grid.Column="1" Content="Clear all history" Margin="8,0,0,0" Padding="10,4"
                    Command="{Binding ClearAllCommand}"/>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Entries}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="3*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="*"/>
                <DataGridTextColumn Header="Downloaded" Binding="{Binding DownloadedAtUtc, StringFormat='{}{0:g}'}" Width="1.4*"/>
                <DataGridTextColumn Header="Size" Binding="{Binding TotalFileSizeBytes, Converter={StaticResource FileSize}}" Width="0.8*"/>
                <DataGridTextColumn Header="Success" Binding="{Binding Success}" Width="0.6*"/>
                <DataGridTemplateColumn Header="Actions" Width="2.4*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Play" Margin="2" Padding="8,2"
                                        Command="{Binding DataContext.PlayCommand, ElementName=RootControl}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="Open folder" Margin="2" Padding="8,2"
                                        Command="{Binding DataContext.OpenFolderCommand, ElementName=RootControl}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="Remove" Margin="2" Padding="8,2"
                                        Command="{Binding DataContext.RemoveCommand, ElementName=RootControl}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
