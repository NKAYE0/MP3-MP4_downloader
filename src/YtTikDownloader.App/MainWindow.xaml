<Window x:Class="YtTikDownloader.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:YtTikDownloader.App.Views"
        xmlns:conv="clr-namespace:YtTikDownloader.App.Converters"
        Title="YouTube / TikTok / YouTube Music Downloader"
        Height="780" Width="1150" MinHeight="560" MinWidth="820">

    <Window.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <DockPanel>

        <!-- Clipboard-detected URL banner -->
        <Border DockPanel.Dock="Top"
                Background="#FFEFF6FF"
                BorderBrush="#FFB6D4FE"
                BorderThickness="0,0,0,1"
                Padding="10,6"
                Visibility="{Binding PendingClipboardUrl, Converter={StaticResource NullToVisibility}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis">
                    <Run Text="Detected in clipboard: "/>
                    <Run Text="{Binding PendingClipboardUrl, Mode=OneWay}" FontWeight="Bold"/>
                </TextBlock>
                <Button Grid.Column="1" Content="Add" Margin="8,0" Padding="10,2"
                        Command="{Binding AddPendingClipboardUrlCommand}"/>
                <Button Grid.Column="2" Content="Dismiss" Padding="10,2"
                        Command="{Binding DismissPendingClipboardUrlCommand}"/>
            </Grid>
        </Border>

        <!-- Status bar -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>

        <TabControl x:Name="MainTabControl" Margin="4">
            <TabItem Header="YouTube">
                <views:CategoryTabView DataContext="{Binding YouTubeTab}"/>
            </TabItem>
            <TabItem Header="TikTok">
                <views:CategoryTabView DataContext="{Binding TikTokTab}"/>
            </TabItem>
            <TabItem Header="YouTube Music">
                <views:CategoryTabView DataContext="{Binding YouTubeMusicTab}"/>
            </TabItem>
            <TabItem x:Name="PlayerTabItem" Header="Player">
                <views:PlayerView x:Name="PlayerViewControl"/>
            </TabItem>
            <TabItem Header="History">
                <views:HistoryView DataContext="{Binding HistoryVm}"/>
            </TabItem>
            <TabItem Header="Statistics">
                <views:StatsView DataContext="{Binding StatsVm}"/>
            </TabItem>
            <TabItem Header="Settings">
                <views:SettingsView DataContext="{Binding SettingsVm}"/>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
